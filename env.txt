# Copy this file to `.env` (same folder) and fill in your real key.
# `.env` itself is git-ignored — never commit it, and never paste a
# real key into a file that lives in the repo (including this one).

# ⚠️ SECURITY: Never paste your real API key here! Use Render's environment variables instead.
# Generate a new key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_new_api_key_here

# Optional overrides
PORT=5000
GEMINI_MODEL=gemini-2.5-flash

# Google Search grounding — lets Gemini search the live web when it
# decides a question needs current info, instead of only answering from
# training data. On by default. Set to false to disable (e.g. to control
# cost): Gemini 2.5 gives 1,500 grounded requests/day free then charges
# roughly $35 per 1,000 grounded prompts; Gemini 3.x models give 5,000/month
# free then ~$14 per 1,000. One message can trigger several search queries,
# each billed separately. See ai.google.dev/gemini-api/docs/pricing.
ENABLE_SEARCH_GROUNDING=true

# CORS: Set to your actual frontend domain(s).
# This prevents your Gemini quota from being used by other websites.
# Set on Render's dashboard → Environment Variables (NOT in this file).
#
# Examples:
# - GitHub Pages: https://dipeshkharel14-hub.github.io
# - Custom domain: https://www.dipeshkharel14.com.np
# - Local testing: http://localhost:3000
# - Multiple origins (comma-separated): https://dipeshkharel14-hub.github.io,https://www.dipeshkharel14.com.np
#
# Leave blank to allow all origins (⚠️ NOT recommended for production):
ALLOWED_ORIGIN=

# Path to index.html for KB loading (defaults to current directory)
INDEX_HTML_PATH=./index.html

# Node environment
NODE_ENV=production
